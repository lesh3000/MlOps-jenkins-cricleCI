apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: sklearn
spec:
  selector:
    matchLabels:
      app: inference
  replicas: 2
  template:
    metadata:
      labels:
        app: inference
    spec:
      containers:
      - name: dmitrylesh
        image: dmitrylesh/preds
        ports:
        - containerPort: 80
