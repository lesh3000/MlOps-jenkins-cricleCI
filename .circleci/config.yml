version: 2.1
parameters:
  accountid:
    type: string
    default: "524768791364"
  region:
    type: string
    default: "us-west-1"
jobs:
  build:
    docker:
      # Use the same Docker base as the project
      - image: python:3.7.3-stretch
    working_directory: ~/proj5
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "requirements.txt" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-
      - run:
          name: install dependencies
          command: |
            python3 -m venv venv
            . venv/bin/activate
            make install
            # Install hadolint
            wget -O /bin/hadolint https://github.com/hadolint/hadolint/releases/download/v1.16.3/hadolint-Linux-x86_64 &&\
                chmod +x /bin/hadolint
      - save_cache:
          paths:
            - ./venv
          key: v1-dependencies-{{ checksum "requirements.txt" }}
      - run:
          name: run lint
          command: |
            . venv/bin/activate
            make -i lint

  pull:
    docker:
      - image: 524768791364.dkr.ecr.us-west-1.amazonaws.com/train
        aws_auth:
          aws_access_key_id: ASIAXULVIJNCI6MI5CNY
          aws_secret_access_key: SDOvdiTi6EEIbXgZR0ZrHAbfg3zaiIbQpY92nGpE
    steps:
      - run:
          name: "Every Day Tests"
          command: "testing...."
      - run:
          name: "Deploy to Staging Infrastructure"
          command: "something something darkside.... cli"
workflows:
  build:
    jobs:
      - build
  pull:
    jobs:
      - pull