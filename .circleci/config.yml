version: 2.1
parameters:
  accountid:
    type: string
    default: "524768791364"
  region:
    type: string
    default: "us-west-1"
jobs:
  build:
    docker:
      # Use the same Docker base as the project
      - image: python:3.7.3-stretch
    working_directory: ~/proj5
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "requirements.txt" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-
      - run:
          name: install dependencies
          command: |
            python3 -m venv venv
            . venv/bin/activate
            make install
            # Install hadolint
            wget -O /bin/hadolint https://github.com/hadolint/hadolint/releases/download/v1.16.3/hadolint-Linux-x86_64 &&\
                chmod +x /bin/hadolint
      - save_cache:
          paths:
            - ./venv
          key: v1-dependencies-{{ checksum "requirements.txt" }}
      - run:
          name: run lint
          command: |
            . venv/bin/activate
            make -i lint

  buildpush:
    docker:
      - image: cimg/python:3.8.7
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.14
          docker_layer_caching: true
      - run: | 
          make buildtrain

  pull:
    docker:
      - image: cimg/python:3.8.7
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.14
          docker_layer_caching: true
      - run:
          name: "do all"
          command: |
            export key_id_=$key_id
            echo $key_id_
  #          docker pull dmitrylesh/train
  #          docker run --rm -it dmitrylesh/train

workflows:
#  build:
#    jobs:
#      - build
  train:
    jobs:
      - pull
      #- buildpush